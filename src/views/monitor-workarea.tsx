import { ReactElement } from "react";
import ScrollArea from '../components/scroll-area';

function monitorWorkarea(): ReactElement {
  return (
    <ScrollArea color="royal-purple">
    </ScrollArea>
  );
}

export default monitorWorkarea;