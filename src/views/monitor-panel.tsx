import { ReactElement } from "react";
import ScrollArea from '../components/scroll-area';

function MonitorPanel(): ReactElement {
  return (
    <ScrollArea color="royal-purple"></ScrollArea>
  );
}

export default MonitorPanel;